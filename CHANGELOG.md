# 更新日志

## 0.9.9 - 2026-06-09

这一版重新梳理设置页里的清理操作，让缓存、临时文件和最近播放记录的删除路径更清楚，也降低误触风险。

### 优化

- 将缓存区清理操作改为“清理最近播放缓存”“清理全部音频缓存”“清理临时播放文件”三类。
- 三个缓存清理按钮统一使用红色危险操作样式。
- “清理最近播放缓存”只清最近播放 100 首带来的缓存，并保留收藏和已选播放列表缓存。
- 资料库里的“清空最近播放”改为“清空最近播放列表”。
- 四个删除类操作都改为居中确认弹窗，用户二次确认后才会执行。

### 版本

- App 版本号设为 `0.9.9`。

## 0.9.8 - 2026-06-09

这一版集中补强 UP 主收藏页、缓存清理和设置页信息展示，重点解决长列表加载、临时文件膨胀和版本可见性问题。

### 新增

- UP 主收藏详情页支持滚动到底部继续加载更多视频。
- 设置页新增“清理临时文件”，可手动删除播放产生的临时音频和陈旧的 URLSession 下载残留。
- 设置页“关于”新增当前 App 版本号显示。

### 优化

- 播放器启动、切歌和停止播放时会自动清理旧临时音频文件。
- 快速切歌时会取消上一首尚未完成的播放下载，减少无效流量和 `tmp` 目录膨胀。
- 临时文件清理会返回释放空间大小，方便确认清理效果。
- UP 主收藏详情页加载更多时显示底部进度提示，并避免筛选时误触发分页请求。

### 版本

- App 版本号设为 `0.9.8`。

## 0.9.7 - 2026-05-27

这一版继续完善“列表即队列”的播放体验，让搜索结果和 UP 主视频列表也能像普通播放列表一样连续播放。

### 新增

- 搜索结果页新增“全部播放”按钮。
- 搜索结果会作为临时播放列表，支持列表循环和随机播放。
- UP 主详情页新增“全部播放”按钮。
- UP 主详情页新增本页筛选框，可在当前视频列表中按歌名简单搜索。

### 优化

- 点击搜索结果中的任意歌曲时，会把当前搜索结果作为播放队列。
- 搜索结果分页加载更多后，会自动追加到当前搜索临时播放队列。
- 点击 UP 主详情页中的任意视频时，会把当前 UP 主列表作为播放队列。
- UP 主详情页筛选后，“全部播放”和单曲播放都会按筛选后的列表生成队列。

### 版本

- App 版本号设为 `0.9.7`。

## 0.9.6 - 2026-05-04

这一版继续打磨首页、播放队列和 UP 主收藏体验，让首页内容更接近原版 eno-M，也让随机播放顺序更可感知。

### 新增

- 首页接入每周 Bilibili 音乐榜，自动读取最新一期榜单。
- 新增 Bilibili 音乐榜公开接口封装：期数列表和单期榜单内容。
- 音乐榜条目支持直接播放、收藏和加入播放列表。

### 优化

- 首页空状态从推荐词云改为每周 Bilibili 音乐榜。
- 从音乐榜或播放列表点击歌曲时，会把整组列表作为当前播放队列。
- 随机播放会生成真实随机队列，播放队列显示和实际播放顺序保持一致。

### 修复

- 修复播放列表中切换随机播放后，播放队列显示不变化的问题。
- 修复 UP 主详情页未收藏时仍显示“取消收藏”的问题。
- 修复取消收藏 UP 主后，当前 UP 主详情页内容被清空的问题。

### 版本

- App 版本号设为 `0.9.6`。

## 0.9.5 - 2026-04-20

这一版集中整理了 B-Music 的原生音乐播放器体验，重点放在播放、资料库、缓存和界面一致性上。

### 新增

- 新增音频缓存：默认保留最近播放的 100 首歌。
- 设置页支持固定缓存“我的收藏”和指定自定义播放列表。
- 设置页新增缓存大小、立即缓存、清理未保留缓存和清空缓存操作。
- 新增列表备份与恢复：每个收藏或播放列表导出为独立文件，支持多文件批量导入。
- 首页推荐关键词改为从 B 站音乐相关排行提取，并支持本地缓存和换一换。
- 首页推荐关键词按热度显示为云图样式。
- 正在播放页支持根据当前封面提取主色，生成渐变、模糊背景，并在切歌时平滑过渡。
- 加入播放列表页改为同款封面取色动态背景。

### 优化

- 统一音乐列表交互：点击列表项直接播放，右侧保留收藏和加入播放列表操作。
- 播放列表、我的收藏、最近播放中点击歌曲时，会把整个列表作为当前播放队列。
- 修复从列表中间开始播放后，下一首应为列表后续歌曲的问题。
- 播放队列显示从当前歌曲开始，和实际播放顺序保持一致。
- 播放列表选择页将已有列表放在上方，新建列表放在下方。
- 播放列表选择页改为更清晰的半透明材质，文字保持高对比度。
- 播放列表详情支持原生编辑模式拖拽排序。
- 我的收藏内取消收藏后不会立即消失，离开页面再回来后刷新。
- 调整正在播放页队列、设置页等滚动性能，减少播放进度更新带来的无关刷新。
- 开启 ProMotion 相关配置，支持高刷新率设备优先使用更高刷新率。
- App 图标更新为更年轻的 B Music 风格。

### 修复

- 修复快速切歌时 UI 当前播放和实际播放不一致的风险。
- 修复正在播放页封面、歌手入口、模式按钮、完成按钮和底部播放栏封面等问题。
- 修复播放栏遮挡底部标签栏的问题。
- 修复资料库页面缺少返回主页路径的问题。
- 修复收藏和加入播放列表按钮逻辑在不同列表中不一致的问题。
- 修复播放列表选择页背景过白、文字不清晰的问题。

### 版本

- App 版本号设为 `0.9.5`。
